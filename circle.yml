general:
  build_dir: src

machine:
  environment:
    APP_ENV: testing
    APP_KEY: base64:/TldazXBjsRBCxmfL7zaNZs7a8nvw41gk7dR1tV+qPY=
    APP_DEBUG: true
    APP_LOG_LEVEL: debug
    APP_URL: http://localhost

    DB_CONNECTION: mysql
    DB_HOST: 127.0.0.1
    DB_PORT: 3306
    DB_DATABASE: circle_test
    DB_USERNAME: ubuntu

    BROADCAST_DRIVER: log
    CACHE_DRIVER: file
    SESSION_DRIVER: file
    QUEUE_DRIVER: sync


dependencies:
  post:
    - php artisan migrate --force --seed